<!DOCTYPE html>
<html lang="ar">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>صاصا ❤️ شوشو</title>
<link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">

<style>
* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  margin: 0;
  font-family: 'Cairo', sans-serif;
  text-align: center;
  background: linear-gradient(135deg, #1a0033 0%, #4a0044 50%, #ff4d6d 100%);
  color: white;
  overflow-x: hidden;
  min-height: 100vh;
}

/* قلوب طايرة */
.heart {
  position: fixed;
  color: rgba(255,255,255,0.4);
  animation: floatUp 8s linear infinite;
  pointer-events: none;
  z-index: 0;
  text-shadow: 0 0 10px rgba(255,77,109,0.5);
}

@keyframes floatUp {
  0% { transform: translateY(100vh) rotate(0deg) scale(0.5); opacity: 0; }
  10% { opacity: 1; }
  90% { opacity: 1; }
  100% { transform: translateY(-10vh) rotate(720deg) scale(1.2); opacity: 0; }
}

.container {
  position: relative;
  z-index: 1;
  margin-top: 20px;
  padding: 20px;
}

/* الأسماء */
.names {
  font-size: 48px;
  font-weight: 700;
  text-shadow: 2px 2px 15px rgba(0,0,0,0.4);
  animation: glow 2s ease-in-out infinite alternate;
  margin-bottom: 5px;
}

@keyframes glow {
  from { text-shadow: 2px 2px 15px rgba(0,0,0,0.4); }
  to { text-shadow: 2px 2px 30px rgba(255,77,109,1), 0 0 50px rgba(255,77,109,0.5); }
}

/* معرض الصور */
.gallery-container {
  width: 320px;
  height: 320px;
  margin: 20px auto;
  position: relative;
  animation: beat 1.5s infinite;
  filter: drop-shadow(0 15px 30px rgba(0,0,0,0.4));
}

.gallery-frame {
  width: 100%;
  height: 100%;
  position: relative;
  overflow: hidden;
  border-radius: 50% 50% 0 50%;
  transform: rotate(-45deg);
  border: 4px solid rgba(255,255,255,0.3);
}

.gallery-frame img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  position: absolute;
  top: 0; left: 0;
  opacity: 0;
  transition: opacity 1s ease-in-out;
  transform: rotate(45deg) scale(1.4);
}

.gallery-frame img.active {
  opacity: 1;
}

@keyframes beat {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.06); }
}

/* مؤشرات الصور */
.gallery-dots {
  margin-top: 15px;
  display: flex;
  justify-content: center;
  gap: 8px;
}

.dot {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: rgba(255,255,255,0.3);
  cursor: pointer;
  transition: all 0.3s;
}

.dot.active {
  background: #ff4d6d;
  transform: scale(1.3);
}

/* العداد */
#counter {
  margin: 20px auto;
  font-size: 22px;
  background: rgba(255,255,255,0.1);
  backdrop-filter: blur(15px);
  padding: 20px 30px;
  border-radius: 25px;
  display: inline-block;
  border: 1px solid rgba(255,255,255,0.2);
  line-height: 2.2;
  box-shadow: 0 8px 32px rgba(0,0,0,0.2);
}

.counter-item {
  display: inline-block;
  margin: 0 8px;
  padding: 5px 12px;
  background: rgba(255,77,109,0.2);
  border-radius: 10px;
}

/* الرسالة العشوائية */
#randomMessage {
  margin: 20px auto;
  font-size: 22px;
  min-height: 60px;
  color: #ffd700;
  text-shadow: 0 0 20px rgba(255,215,0,0.5);
  font-weight: 700;
}

.typing-cursor {
  display: inline-block;
  width: 3px;
  height: 25px;
  background: #ffd700;
  margin-right: 5px;
  animation: blink 0.7s infinite;
  vertical-align: middle;
}

@keyframes blink {
  0%, 100% { opacity: 1; }
  50% { opacity: 0; }
}

/* زر بحبك */
.love-button-container {
  margin: 20px 0;
}

#loveCounter {
  font-size: 28px;
  margin: 10px 0;
  color: #ff4d6d;
  font-weight: 700;
}

.love-btn {
  font-size: 24px !important;
  padding: 15px 40px !important;
  background: linear-gradient(45deg, #ff0040, #ff4d6d, #ff758c) !important;
  animation: lovePulse 1.5s infinite !important;
}

@keyframes lovePulse {
  0%, 100% { box-shadow: 0 0 20px rgba(255,0,64,0.5); }
  50% { box-shadow: 0 0 40px rgba(255,0,64,0.8), 0 0 60px rgba(255,77,109,0.4); }
}

/* الأزرار */
.buttons {
  margin-top: 20px;
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 10px;
}

button {
  padding: 12px 25px;
  border: none;
  border-radius: 25px;
  background: linear-gradient(45deg, #ff4d6d, #ff758c, #ff4d6d);
  background-size: 200% 200%;
  color: white;
  font-size: 16px;
  font-family: 'Cairo', sans-serif;
  font-weight: 700;
  cursor: pointer;
  transition: all 0.3s ease;
  box-shadow: 0 6px 20px rgba(255,77,109,0.4);
  animation: gradientMove 3s ease infinite;
}

@keyframes gradientMove {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}

button:hover {
  transform: translateY(-3px) scale(1.05);
  box-shadow: 0 10px 30px rgba(255,77,109,0.6);
}

button:active {
  transform: translateY(0) scale(0.98);
}

/* الرسالة الثابتة */
#loveMessage {
  display: none;
  margin-top: 20px;
  font-size: 20px;
  background: rgba(255,255,255,0.1);
  backdrop-filter: blur(10px);
  padding: 20px;
  border-radius: 20px;
  max-width: 550px;
  margin-left: auto;
  margin-right: auto;
  line-height: 2;
  animation: fadeIn 0.6s ease;
  border: 1px solid rgba(255,255,255,0.15);
}

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(-20px); }
  to { opacity: 1; transform: translateY(0); }
}

/* مؤشر الموسيقى */
.music-indicator {
  position: fixed;
  top: 20px;
  right: 20px;
  font-size: 28px;
  opacity: 0;
  transition: opacity 0.5s;
  z-index: 100;
  background: rgba(255,77,109,0.3);
  padding: 10px 15px;
  border-radius: 50%;
  backdrop-filter: blur(10px);
}

.music-indicator.active {
  opacity: 1;
  animation: pulse 1s infinite, bounce 2s infinite;
}

@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.3); }
}

@keyframes bounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10px); }
}

/* Equalizer */
.equalizer {
  display: none;
  position: fixed;
  bottom: 20px;
  left: 50%;
  transform: translateX(-50%);
  gap: 4px;
  z-index: 100;
}

.equalizer.active { display: flex; }

.bar {
  width: 6px;
  background: linear-gradient(to top, #ff4d6d, #ff758c);
  border-radius: 3px;
  animation: equalize 1s ease-in-out infinite;
}

.bar:nth-child(1) { height: 20px; animation-delay: 0s; }
.bar:nth-child(2) { height: 35px; animation-delay: 0.1s; }
.bar:nth-child(3) { height: 50px; animation-delay: 0.2s; }
.bar:nth-child(4) { height: 40px; animation-delay: 0.3s; }
.bar:nth-child(5) { height: 30px; animation-delay: 0.4s; }
.bar:nth-child(6) { height: 45px; animation-delay: 0.5s; }
.bar:nth-child(7) { height: 25px; animation-delay: 0.6s; }

@keyframes equalize {
  0%, 100% { transform: scaleY(0.5); }
  50% { transform: scaleY(1.2); }
}

/* معلومات الأغنية */
.song-info {
  position: fixed;
  bottom: 20px;
  left: 20px;
  background: rgba(0,0,0,0.4);
  backdrop-filter: blur(10px);
  padding: 10px 20px;
  border-radius: 20px;
  font-size: 14px;
  opacity: 0;
  transition: opacity 0.5s;
  z-index: 100;
}

.song-info.active { opacity: 1; }

/* Responsive */
@media (max-width: 600px) {
  .names { font-size: 32px; }
  .gallery-container { width: 260px; height: 260px; }
  #counter { font-size: 16px; padding: 15px 20px; }
  #randomMessage { font-size: 18px; }
  button { font-size: 14px; padding: 10px 18px; }
}
</style>
</head>

<body>

<!-- مؤشر الموسيقى -->
<div class="music-indicator" id="musicIndicator">🎵</div>

<!-- Equalizer -->
<div class="equalizer" id="equalizer">
  <div class="bar"></div><div class="bar"></div><div class="bar"></div>
  <div class="bar"></div><div class="bar"></div><div class="bar"></div>
  <div class="bar"></div>
</div>

<!-- معلومات الأغنية -->
<div class="song-info" id="songInfo">🎶 عمرو دياب - تملي معاك</div>

<div class="container">

<div class="names">صاصا ❤️ شوشو</div>

<!-- معرض الصور المتحرك -->
<div class="gallery-container">
  <div class="gallery-frame" id="galleryFrame">
    <!-- صور من Unsplash - مجانية للاستخدام [^3^] -->
    <img src="https://images.unsplash.com/photo-1516589178581-6cd7833ae3b2?w=400&h=400&fit=crop" class="active" alt="صورة 1">
    <img src="https://images.unsplash.com/photo-1529333166437-7750a6dd5a70?w=400&h=400&fit=crop" alt="صورة 2">
    <img src="https://images.unsplash.com/photo-1518621736915-f3b1c41bfd00?w=400&h=400&fit=crop" alt="صورة 3">
    <img src="https://images.unsplash.com/photo-1522673607200-164d1b6ce486?w=400&h=400&fit=crop" alt="صورة 4">
    <img src="https://images.unsplash.com/photo-1474552226712-ac0f0961a954?w=400&h=400&fit=crop" alt="صورة 5">
  </div>
</div>

<!-- مؤشرات الصور -->
<div class="gallery-dots" id="galleryDots"></div>

<!-- الرسالة العشوائية مع تأثير الكتابة -->
<div id="randomMessage"><span class="typing-cursor"></span></div>

<!-- العداد -->
<div id="counter"></div>

<!-- زر بحبك -->
<div class="love-button-container">
  <button class="love-btn" onclick="addLove()">❤️ بحبك ❤️</button>
  <div id="loveCounter">0 ❤️</div>
</div>

<div class="buttons">
  <button onclick="toggleMusic()">🎵 تشغيل / إيقاف</button>
  <button onclick="showMessage()">💌 رسالة حب</button>
  <button onclick="surprise()">😏 مفاجأة</button>
  <button onclick="newMessage()">✨ رسالة جديدة</button>
  <button onclick="fireworks()">🎆 ألعاب نارية</button>
</div>

<div id="loveMessage">
  من يوم 14/12/2023 ❤️<br>
  وأنا بحبك أكتر كل يوم 💖<br>
  أنتِ أجمل ما في حياتي 🌹<br>
  ودايماً هفضل جنبك 💕
</div>

</div>

<audio id="music" loop>
  <source src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3" type="audio/mpeg">
</audio>

<script>
/* ========== معرض الصور ========== */
const galleryImages = document.querySelectorAll('.gallery-frame img');
const galleryDots = document.getElementById('galleryDots');
let currentImage = 0;

// إنشاء النقاط
galleryImages.forEach((_, i) => {
  const dot = document.createElement('div');
  dot.className = 'dot' + (i === 0 ? ' active' : '');
  dot.onclick = () => showImage(i);
  galleryDots.appendChild(dot);
});

function showImage(index) {
  galleryImages[currentImage].classList.remove('active');
  document.querySelectorAll('.dot')[currentImage].classList.remove('active');
  currentImage = index;
  galleryImages[currentImage].classList.add('active');
  document.querySelectorAll('.dot')[currentImage].classList.add('active');
}

// تقلب تلقائي كل 4 ثواني
setInterval(() => {
  showImage((currentImage + 1) % galleryImages.length);
}, 4000);

/* ========== رسائل عشوائية + Typewriter ========== */
const loveMessages = [
  "أنتِ أحلى حاجة حصلتلي ❤️",
  "بحبك أكتر من كلمة ❤️",
  "عينيكِ أجمل مكان في الدنيا 🌹",
  "كل يوم معاكِ بيبقى أجمل من اللي قبله 💖",
  "أنتِ حياتي وكل حاجة حلوة فيها ❤️",
  "تملي معاك... وأفضل معاك ❤️🎵",
  "قلبي بيتكلم بيكِ وبس 💕",
  "مفيش يوم بيعدي من غير ما فكر فيكِ 💭❤️",
  "أنتِ نصي التاني اللي بيكملني 💑",
  "بحلم بيكِ كل ليلة 🌙❤️",
  "ضحكتكِ أجمل موسيقى بسمعها 🎵",
  "لو الدنيا كلها ضدي، أنا معاكِ 💪❤️",
  "أنتِ السبب اللي بخاف أخسره 😍",
  "مش عايز غيركِ في حياتي 🌹",
  "كل لحظة معاكِ بتسوى الدنيا 💎",
  "أنتِ الأمان اللي بطلبه 🤗❤️",
  "بحبك من قلبي لقلبك 💕",
  "عمري ما هسيب إيدكِ 🤝❤️",
  "أنتِ الحب اللي كنتُ بدور عليه 🔍❤️",
  "معاكِ الدنيا حلوة أوي 🌍💖"
];

let typewriterTimeout;
let currentMessageIndex = -1;

function typeWriter(text, element, speed = 80) {
  clearTimeout(typewriterTimeout);
  element.innerHTML = '<span class="typing-cursor"></span>';
  let i = 0;
  
  function type() {
    if (i < text.length) {
      element.innerHTML = text.substring(0, i + 1) + '<span class="typing-cursor"></span>';
      i++;
      typewriterTimeout = setTimeout(type, speed);
    } else {
      element.innerHTML = text + '<span class="typing-cursor"></span>';
    }
  }
  type();
}

function newMessage() {
  let newIndex;
  do {
    newIndex = Math.floor(Math.random() * loveMessages.length);
  } while (newIndex === currentMessageIndex);
  
  currentMessageIndex = newIndex;
  typeWriter(loveMessages[newIndex], document.getElementById('randomMessage'));
}

// رسالة أولى عند التحميل
window.onload = () => {
  newMessage();
};

/* ========== العداد ========== */
const startDate = new Date("2023-12-14T00:00:00");

function updateCounter() {
  const now = new Date();
  const diff = now - startDate;
  const seconds = Math.floor(diff / 1000);
  const days = Math.floor(seconds / (3600 * 24));
  const hours = Math.floor((seconds % (3600 * 24)) / 3600);
  const minutes = Math.floor((seconds % 3600) / 60);
  const sec = seconds % 60;

  document.getElementById("counter").innerHTML =
    `<span class="counter-item">💕 ${days} يوم</span><br>
     <span class="counter-item">⏳ ${hours} ساعة</span>
     <span class="counter-item">⏰ ${minutes} دقيقة</span>
     <span class="counter-item">⏱️ ${sec} ثانية</span>`;
}

setInterval(updateCounter, 1000);
updateCounter();

/* ========== موسيقى ========== */
const music = document.getElementById("music");
const indicator = document.getElementById("musicIndicator");
const equalizer = document.getElementById("equalizer");
const songInfo = document.getElementById("songInfo");

function toggleMusic() {
  if (music.paused) {
    music.play().then(() => {
      indicator.classList.add("active");
      equalizer.classList.add("active");
      songInfo.classList.add("active");
    }).catch(() => {
      alert("🔇 دوس على الزر تاني عشان تشغل الموسيقى!");
    });
  } else {
    music.pause();
    indicator.classList.remove("active");
    equalizer.classList.remove("active");
    songInfo.classList.remove("active");
  }
}

/* ========== رسالة ثابتة ========== */
function showMessage() {
  const msg = document.getElementById("loveMessage");
  msg.style.display = msg.style.display === "block" ? "none" : "block";
}

/* ========== زر بحبك ========== */
let loveCount = 0;
function addLove() {
  loveCount++;
  document.getElementById("loveCounter").textContent = loveCount + " ❤️";
  
  // تأثير قلوب بتطير
  for(let i = 0; i < 10; i++) {
    setTimeout(() => {
      const heart = document.createElement("div");
      heart.innerHTML = "❤️";
      heart.style.position = "fixed";
      heart.style.left = (50 + (Math.random() - 0.5) * 30) + "%";
      heart.style.top = "50%";
      heart.style.fontSize = (Math.random() * 20 + 20) + "px";
      heart.style.animation = "floatUp 3s ease-out forwards";
      heart.style.pointerEvents = "none";
      heart.style.zIndex = "1000";
      document.body.appendChild(heart);
      setTimeout(() => heart.remove(), 3000);
    }, i * 50);
  }
  
  // صوت قلب بيدق (باستخدام Web Audio API)
  playHeartbeat();
}

function playHeartbeat() {
  try {
    const audioCtx = new (window.AudioContext || window.webkitAudioContext)();
    const oscillator = audioCtx.createOscillator();
    const gainNode = audioCtx.createGain();
    
    oscillator.connect(gainNode);
    gainNode.connect(audioCtx.destination);
    
    oscillator.frequency.value = 800;
    gainNode.gain.setValueAtTime(0.3, audioCtx.currentTime);
    gainNode.gain.exponentialRampToValueAtTime(0.01, audioCtx.currentTime + 0.1);
    
    oscillator.start(audioCtx.currentTime);
    oscillator.stop(audioCtx.currentTime + 0.1);
    
    // دقة تانية
    setTimeout(() => {
      const osc2 = audioCtx.createOscillator();
      const gain2 = audioCtx.createGain();
      osc2.connect(gain2);
      gain2.connect(audioCtx.destination);
      osc2.frequency.value = 800;
      gain2.gain.setValueAtTime(0.3, audioCtx.currentTime);
      gain2.gain.exponentialRampToValueAtTime(0.01, audioCtx.currentTime + 0.1);
      osc2.start(audioCtx.currentTime);
      osc2.stop(audioCtx.currentTime + 0.1);
    }, 200);
  } catch(e) {}
}

/* ========== مفاجأة ========== */
function surprise() {
  const messages = [
    "أنتِ أحلى حاجة حصلتلي ❤️",
    "بحبك أكتر من كلمة ❤️",
    "عينيكِ أجمل مكان في الدنيا 🌹",
    "كل يوم معاكِ بيبقى أجمل من اللي قبله 💖",
    "أنتِ حياتي وكل حاجة حلوة فيها ❤️",
    "تملي معاك... وأفضل معاك ❤️🎵",
    "قلبي بيتكلم بيكِ وبس 💕"
  ];
  const randomMsg = messages[Math.floor(Math.random() * messages.length)];
  
  for(let i = 0; i < 50; i++) {
    setTimeout(createHeart, i * 30);
  }
  
  alert(randomMsg);
}

/* ========== ألعاب نارية ========== */
function fireworks() {
  const colors = ["❤️", "💖", "💕", "💗", "💝", "✨", "🌟", "🔥", "🎆", "🌹"];
  for(let i = 0; i < 100; i++) {
    setTimeout(() => {
      const heart = document.createElement("div");
      heart.innerHTML = colors[Math.floor(Math.random() * colors.length)];
      heart.style.position = "fixed";
      heart.style.left = Math.random() * 100 + "vw";
      heart.style.top = Math.random() * 100 + "vh";
      heart.style.fontSize = (Math.random() * 30 + 20) + "px";
      heart.style.animation = "explode 2s ease-out forwards";
      heart.style.pointerEvents = "none";
      heart.style.zIndex = "1000";
      document.body.appendChild(heart);
      setTimeout(() => heart.remove(), 2000);
    }, i * 15);
  }
}

/* ========== قلوب طايرة ========== */
function createHeart() {
  const heart = document.createElement("div");
  heart.className = "heart";
  heart.innerHTML = ["❤️", "💖", "💕", "💗", "💝"][Math.floor(Math.random() * 5)];
  heart.style.left = Math.random() * 100 + "vw";
  heart.style.fontSize = (Math.random() * 25 + 15) + "px";
  heart.style.animationDuration = (Math.random() * 4 + 4) + "s";
  document.body.appendChild(heart);
  setTimeout(() => heart.remove(), 8000);
}

setInterval(createHeart, 300);

// تأثير انفجار
const style = document.createElement("style");
style.textContent = `
@keyframes explode {
  0% { transform: scale(0) rotate(0deg); opacity: 1; }
  50% { transform: scale(1.5) rotate(180deg); opacity: 1; }
  100% { transform: scale(0) rotate(360deg); opacity: 0; }
}
`;
document.head.appendChild(style);
</script>

</body>
</html>

